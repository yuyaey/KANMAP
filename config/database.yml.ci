test:
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: <%= ENV.fetch("MYSQL_USER", "root") %>
  password: <%= ENV.fetch("MYSQL_ROOT_PASSWORD", "136445yY$") %>
  socket: /tmp/mysql.sock
  database: KANMAP_test
  host: <%= ENV.fetch("MYSQL_ROOT_HOST", "127.0.0.1") %>
  port: <%= ENV.fetch("MYSQL_PORT", "3306") %>